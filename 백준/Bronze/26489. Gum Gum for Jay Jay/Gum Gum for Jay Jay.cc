#include <bits/stdc++.h>
using namespace std;
int main() {
    ios::sync_with_stdio(0); cin.tie(0);
    int ans = 0; string s;
    while (getline(cin, s)) ans++;
    cout << ans << "\n";
    return 0;
}